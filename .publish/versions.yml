# Maps release group placeholders to their current version.
# The git-flow release process reads and bumps this file.
#
# Starts at 0.2.0: the predecessor package @heroesbrowser/heroprotocol published
# 0.1.2 to the public npm registry in 2018. The first git-flow release must move
# forward from that, not collide with it.
0.0.0-MAIN: 0.2.0
